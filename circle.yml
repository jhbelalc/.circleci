version: 2
jobs:
   build:
     docker:
       - image: circleci/php:5.6
     steps:
       - checkout
       
      - store_artifacts:
          path: test-reports/
          destination: tr1
      - store_test_results:
          path: test-reports/       
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/test-reports
    - mkdir -p $CIRCLE_ARTIFACTS/test-reports
    - phpunit --testdox-html $CIRCLE_ARTIFACTS/test-reports/index.html --bootstrap Email.php Emailtest.php 
    - echo "hola"
