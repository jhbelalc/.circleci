version: 2
jobs:
   build:
     docker:
       - image: circleci/php:5.6
     steps:
       - checkout
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/phpunit
    - phpunit --bootstrap Email.php Emailtest.php --testdox-html $CIRCLE_TEST_REPORTS/phpunit/index.html 
    - echo "hola"
  post:
    - mkdir $CIRCLE_ARTIFACTS/phpunit
    - mv solo/target/*.html $CIRCLE_ARTIFACTS/phpunit

    
